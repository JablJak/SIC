dataset:
  module: src.data.imagenet_dataset.ImageNetDataset
  transform:
    - module: src.data.transforms.RGBCompression
      args: {}
  args:
    variant: train.X1
    subset_classes: 5
    subset_samples: 200

model:
  module: src.models.swin_compression.SwinTransformerCompressionAutoencoder
  args:
    pretrained_encoder: true

dataloader:
  args:
    batch_size: 16
    shuffle: true
    num_workers: 8

optimizer:
  module: torch.optim.AdamW
  args:
    lr: 0.0001

scheduler:
  module: torch.optim.lr_scheduler.CyclicLR
  enabled: true
  args:
    base_lr: 0.0001
    max_lr: 0.001
    mode: triangular2
    step_size_up: 10
    step_size_down: 40

loss:
  module: src.losses.mse_ssim.MSESSIM
  args:
    alpha: 0

rd_loss:
  enabled: true
  lambda: 50000

epochs: 500

dataset_split_ratio: 0.7

output:
  model:
    name: SWIN-T-IC
    version: 0.4.2

project_name: INZ
task_name: v0.4.2 SWIN Transformer Autoencoder based Image Compression
comment: |
  0.4.0 on RGB